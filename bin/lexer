#!/usr/bin/env/ node

'use strict';

const fs = require('fs');
const Lexer = require('../lib/lexer');
const src = process.argv[2];
const dest = process.argv[3];

const lexem = Lexer.analize(src);

fs.writeFileSync(dest, JSON.stringify(lexem));
